// import { getIconComponent } from '../icons';

( function ( $ ) {
	class FWPListivo_Frontend {
		/**
		 * Constructor
		 */
		constructor() {
			this.init();
		}
		init() {
			const thisClass = this;var mapInterval;
			// console.log( getIconComponent( 'QuickLogin' ) );
		}
	}
	new FWPListivo_Frontend();
} )( ( typeof jQuery !== 'undefined' ) ? jQuery : false );
